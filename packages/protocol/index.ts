/**
 * Public surface of the protocol package.
 *
 * Consumers import schemas and types from here and never reach into `generated/` directly,
 * so the layout of the generated tree stays an internal detail.
 */
export * from './generated/typescript/protocol.js';
